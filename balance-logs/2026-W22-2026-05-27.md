# 2026-W22 / 2026-05-27 Daily Balance and Card Log

## Scope

- Run date: 2026-05-27 (Wednesday, Asia/Tokyo).
- Thursday new-theme rule did not apply.
- Lore file used: `docs/theme-stories.md`.
- Existing theme selected from pre-change CPU approximation: `星導`.

## Cards Added

- `star_surveyor_noll` / `星導の測星係ノル`
  - Theme: `星導`
  - Type: unit, cost 1, ATK 900.
  - Role: low-stat setup unit that can convert a `星導` hand card into charge after the player has used the normal charge action.
  - Optional wording: `通常召喚時：この効果を発動できる。` and `追加で1枚ドローできる。`
  - Art: `assets/cards/art/star-surveyor-noll.png`.
- `star_observation_record` / `星導の観測記録`
  - Theme: `星導`
  - Type: spell, cost 1.
  - Role: searches a `星導` core, with a higher-charge optional draw.
  - Activation wording: `発動時：この効果を発動する。`
  - Optional wording: `追加で1枚ドローできる。`
  - Art: `assets/cards/art/star-observation-record.png`.
- `generic_watch_drone` / `汎用警戒ドローン`
  - Theme: generic.
  - Type: unit, cost 2, ATK 2200, no effect.
  - Role: moderate neutral body between existing 2100 and 2300 cost-2 vanilla units.
  - Art: `assets/cards/art/generic-watch-drone.png`.

## Lore and Pack/Gacha

- Added a `星導` story extension for 測星係ノル and 観測記録 in `docs/theme-stories.md`.
- Verified `星導パック` is auto-derived and includes both new `星導` cards.
- Pack cover reused: `assets/packs/star-pack.png`.
- Verified `generic_watch_drone` enters the shared pack pool through `cardPool`.
- No new theme pack or cover was required because this was not a new theme.

## CPU Battle Results

- Pre-change approximation from this run, before edits:
  - 240 mirrored games completed, no crashes.
  - `星導`: 22-74, avg LP margin -4157.3.
  - `黒機`: 52-44, avg LP margin +810.4.
  - `断刃`: 35-61, avg LP margin -2913.5.
  - `電脳`: 73-23, avg LP margin +3950.0.
  - `双彩`: 58-38, avg LP margin +2310.4.
- Post-change smoke approximation after applying the changes on top of local `main`, fixing the local `reactionRevealed` crash, and stubbing animation timers:
  - 240 mirrored games completed, no crashes.
  - `星導`: 67-29, avg LP margin +2102.1.
  - `黒機`: 32-64, avg LP margin -1661.5.
  - `断刃`: 50-46, avg LP margin +327.1.
  - `電脳`: 24-72, avg LP margin -2449.0.
  - `双彩`: 67-29, avg LP margin +1681.3.
- Same-harness baseline check with the old `星導` deck shape:
  - `星導`: 64-32, avg LP margin +2541.7.
- Balance conclusion:
  - The current harness is useful for crash detection but is too sensitive to timing stubs, random order, and bug-fix side effects for precise balance.
  - The final `星導` preset uses only one copy of each new card and was slightly below the old `星導` deck shape in the same immediate-timer harness.

## Bugs and Text Fixes

- Fixed local crash for `電脳の裏回線`: `EffectResolver` called `this.game.reactionRevealed`, but `DuelGame` only exposed a private helper. Added a `DuelGame.reactionRevealed` instance method.
- Clarified `電脳の裏回線` card text with `発動時：この効果を発動する。` so spell activation wording matches implementation.
- Added renderer support for existing `星` attribute by mapping it to the light attribute class, covering `双彩のルリ` and `双彩のルナ`.

## Japanese and Furigana Review

- Added ruby terms:
  - `測星係` -> `そくせいがかり`
  - `観測記録` -> `かんそくきろく`
  - `汎用警戒ドローン` -> `はんようけいかいどろーん`
- Suspicious item for later review:
  - `src/data/cards.js`: `sosai_ruri` and `sosai_luna` use `attr: "星"`. Renderer support was added, but confirm whether `星` is intended as a formal attribute or whether those cards should use one of the original attributes.

## Validation

- Passed:
  - `node --check src\data\cards.js`
  - `node --check src\core\effect-resolver.js`
  - `node --check src\core\duel-game.js`
  - `node --check src\ui\card-renderer.js`
  - `node --check server.js`
  - Data validation: duplicate IDs, missing art, missing attr class, unknown deck IDs, effect key coverage, activation wording for new cards.
  - Deck validation: starter deck 40, starter drive deck 10, all CPU decks 40/10.
  - Pack validation: `星導パック` includes new `星導` cards, generic card is in `cardPool`.
  - Image dimensions: all three generated images are 1254x1254.
  - CPU smoke: post-change mirrored harness completed with no crashes.
- Not used as validation:
  - `node --check index.html` was attempted once and failed because Node does not syntax-check `.html` files.

## Commit and Push

- Initial attempt was blocked because Git could not write the worktree index/admin directory at `C:\Users\user\Desktop\card-game\.git\worktrees\card-game2\index.lock` (`Permission denied`) while trying to replay the detached worktree changes onto local `main`.
- Follow-up push was requested by the user and the changes were replayed onto the local `main` commit in detached mode because the `main` branch itself was already checked out by another worktree.
- `index.html` had a cache-buster conflict with the latest `main` selection-flicker fix; resolved by keeping the upstream duel-view cache-buster and combining the card-renderer cache-buster.
- Commit/push result: committed as `0f99e0c` (`Add modest Star support cards`) and pushed to `origin/main` on 2026-05-27 after validation passed.

## Files Changed

- `assets/cards/art/generic-watch-drone.png`
- `assets/cards/art/star-observation-record.png`
- `assets/cards/art/star-surveyor-noll.png`
- `docs/theme-stories.md`
- `index.html`
- `server.js`
- `src/core/duel-game.js`
- `src/core/effect-resolver.js`
- `src/data/cards.js`
- `src/ui/card-renderer.js`
- `balance-logs/2026-W22-2026-05-27.md`

## Follow-Up Risks

- The CPU harness should be improved before making large balance moves from win rates.
- The `星` attribute should be confirmed as intentional design vocabulary.
